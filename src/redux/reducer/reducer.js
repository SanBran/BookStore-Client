let initialState = {};

const reducer = (state = initialState, action) => {
  // switch (action.type) {
  //   case GET:
  //     return {
  //       ...state,
  //     };
  //   default:
  //     return { ...state };
  // }
};

export default reducer;
